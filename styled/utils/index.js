export const getBackground = (index) => {
    switch(index) {
        case 0:
            return '#f8fafe';
        case 1:
            return '#ffffff';
        case 2:
            return '#f0f3f8';
        default: 
            return "#ffffff";
    }
};

export const colors = {
    textDefault: '#1c2b3e',
    textMedium: '#515c6b',
    textWeak: '#c2c5d1',
};